/*

    @Request(), @Req()  -- stands for --    req
    @Response(), @Res() -- stands for --    res
    @Next()             -- stands for --    next
    @Sesssion()         -- stands for --    session
    @Param(param? : string) -- stands for --    req.params / req.params[param]
    @Body(param? : string)  -- stands for --    req.body / req.body[param]
    @Query(param? : string) -- stands for --    req.query / req.query[param]
    @Headers(param? : string)   -- stands for --    req.headers / req.headers[param]
    @Ip()               -- stands for --    req.ip
    @HostParam()        -- stands for --    req.hosts

*/